import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  standalone: true,
  selector: 'app-flip-btn',
  imports: [RouterModule],
  templateUrl: './flip-btn.html',
  styleUrl: './flip-btn.css',
})
export class FlipBtn {

}
