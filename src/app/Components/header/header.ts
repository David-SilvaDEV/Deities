import { Component } from '@angular/core';
import { Botones } from '../botones/botones';

@Component({
  selector: 'app-header',
  imports: [Botones ],
  templateUrl: './header.html',
  styleUrl: './header.css',
})
export class Header {

}
